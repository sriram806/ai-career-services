{
  "name": "@ai-career-os/database",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/ --ext .ts",
    "lint:fix": "eslint src/ --ext .ts --fix"
  },
  "dependencies": {
    "@ai-career-os/logger": "*",
    "ioredis": "^5.4.0",
    "mongoose": "^8.9.0",
    "pg": "^8.13.0",
    "drizzle-orm": "^0.37.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "drizzle-kit": "^0.30.0",
    "rimraf": "^6.0.0",
    "typescript": "^5.7.0"
  }
}
